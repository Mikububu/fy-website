# macOS files
.DS_Store
._*

# Node modules (if needed later)
node_modules/

# Netlify
.netlify/

# Large screenshots (not needed for deployment)
screencapture.png
screencapture*.jpg

# Environment variables (NEVER commit these!)
.env
.env.local
.env.production

# Package lock files
package-lock.json
yarn.lock
